apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: default
  labels:
    app: auth-service
spec:
  type: ClusterIP
  selector:
    app: auth-api
  ports:
  - name: http
    port: 4001
    targetPort: 4001
    protocol: TCP
