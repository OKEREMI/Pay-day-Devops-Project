apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: default
  labels:
    app: payment-service
spec:
  type: ClusterIP
  selector:
    app: payment-api
  ports:
  - name: http
    port: 4002
    targetPort: 4002
    protocol: TCP
